const SuccessNoti = ({ message }) => {
  return (
    <div style={{ background: "white", color: "green", borderColor: "red" }}>
      <header>{message}</header>
    </div>
  );
};

export default SuccessNoti;
